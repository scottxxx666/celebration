export const GAME_WIDTH = 800;
export const GAME_HEIGHT = 400;

export const PLAYER_X = 150;

// World scroll speed (pixels/second)
export const MIN_SPEED = 120;
export const MAX_SPEED = 600;
export const ACCEL_STEP = 90;   // speed gained per alternating tap
export const DECEL_PER_SEC = 60; // speed lost per second when not tapping

// Player vertical movement
export const VERTICAL_SPEED = 250; // pixels/second

// Obstacle spawning
export const SPAWN_INTERVAL_MS = 1400;
